{
  "name": "angelo-coelho-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "generate-images": "node generate-images.js",
    "update:resume": "git submodule update --init --remote resume || true",
    "update:siemens": "git submodule update --init --remote siemens || true",
    "build:resume": "node scripts/build-resume.js",
    "build:siemens": "mkdir -p dist/siemens && cp -r siemens-prebuilt/* dist/siemens/",
    "build:fintrack": "cd ../fintrack/frontend && npm ci --omit=dev && VITE_API_URL=https://fintrack-api.railway.app VITE_BASE_PATH=/poc/fintrack/ npm run build && mkdir -p ../../angelorscoelho.dev/dist/fintrack && xcopy /E /Y dist\\* ../../angelorscoelho.dev/dist\\fintrack\\",
    "prebuild": "npm run generate-images && npm run update:resume && npm run build:resume && npm run update:siemens",
    "build": "npm run prebuild && tsc && vite build && npm run build:siemens && npm run build:fintrack",
    "preview": "vite preview"
  },
  "dependencies": {
    "@google/genai": "^1.34.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@types/react": "^19.2.3",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.3",
    "vite": "^6.1.0"
  }
}
